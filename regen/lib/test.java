class T {
	static { int i; }

	public T() {
		this( (int) (r * 255), (int) (g * 255));
	}
	void bar(int x, int y, int z) {
	    return;
	}

	void foo() {
		Class c = (int.class);
		final class U { int i; }
		Class c = int[].class;
		Class d = Object[].class;
		t.new T();
		((T)t).method();
		return "[i=" + (value) + "]";
		int q = (int)+3;
		int z = (int)4;
		int y = (z)+5;
		String s = (String) "ff";
		String t = (s)+"blort";
	}
}
